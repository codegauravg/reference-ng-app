import { ActFilterPipe } from './act-filter.pipe';

describe('ActFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ActFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
